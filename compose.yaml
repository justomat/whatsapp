services:
  api:
    build:
      context: .
    entrypoint: php artisan octane:frankenphp --workers=1 --max-requests=1
    ports:
      - "8000:8000"
    volumes:
      - .:/app

  cache:
    image: valkey/valkey
    ports:
      - "6379:6379"
    environment:
      - REDIS_PASSWORD=secret
